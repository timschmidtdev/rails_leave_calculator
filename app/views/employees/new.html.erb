 <h1>New Employee</h1>
 
 <div class="row">
   <div class="col-md-4">
     <p>Guidelines for creating employees:</p>
     <ul>
       <li>Employees can have as many leave plans as they need.</li>
     </ul>
   </div>
   <div class="col-md-8">
     <%= form_for @employee do |f| %>
       <div class="form-group">
         <%= f.label :name %>
         <%= f.text_field :name, class: 'form-control', placeholder: "Employee name" %>
       </div>
       <%= f.submit "Save", class: 'btn btn-success' %>
     <% end %>
   </div>
 </div>
